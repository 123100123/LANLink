diff --git a/node_modules/expo-router/build/fork/useLinking.native.js b/node_modules/expo-router/build/fork/useLinking.native.js
index 23fff02..ee042ef 100644
--- a/node_modules/expo-router/build/fork/useLinking.native.js
+++ b/node_modules/expo-router/build/fork/useLinking.native.js
@@ -65,17 +65,19 @@ function useLinking(ref, { enabled = true, prefixes, filter, config, getInitialU
         if (independent) {
             return undefined;
         }
-        if (enabled !== false && linkingHandlers.length) {
-            console.error([
-                'Looks like you have configured linking in multiple places. This is likely an error since deep links should only be handled in one place to avoid conflicts. Make sure that:',
-                "- You don't have multiple NavigationContainers in the app each with 'linking' enabled",
-                '- Only a single instance of the root component is rendered',
-                react_native_1.Platform.OS === 'android'
-                    ? "- You have set 'android:launchMode=singleTask' in the '<activity />' section of the 'AndroidManifest.xml' file to avoid launching multiple instances"
-                    : '',
-            ]
-                .join('\n')
-                .trim());
+        // Backport of expo/expo#42644: on Android the root component legitimately
+        // remounts into the surviving JS context after activity recreation, so a
+        // single stale handler is expected there and not a configuration error.
+        if (enabled !== false && linkingHandlers.length && process.env.EXPO_OS !== 'android') {
+            if (linkingHandlers.length > 1) {
+                console.error([
+                    'Looks like you have configured linking in multiple places. This is likely an error since deep links should only be handled in one place to avoid conflicts. Make sure that:',
+                    "- You don't have multiple NavigationContainers in the app each with 'linking' enabled",
+                    '- Only a single instance of the root component is rendered',
+                ]
+                    .join('\n')
+                    .trim());
+            }
         }
         const handler = Symbol();
         if (enabled !== false) {
diff --git a/node_modules/expo-router/build/global-state/router-store.js b/node_modules/expo-router/build/global-state/router-store.js
index 05ad58f..a345cb1 100644
--- a/node_modules/expo-router/build/global-state/router-store.js
+++ b/node_modules/expo-router/build/global-state/router-store.js
@@ -189,6 +189,15 @@ function useStore(context, linkingConfigOptions, serverUrl) {
         // In development, we will show the onboarding screen
         rootComponent = react_1.Fragment;
     }
+    // Backport of expo/expo#42644 ([expo-router] fix linking in two places and
+    // state restoration after activity recreation). On Android the Hermes context
+    // (and this module's storeRef) can outlive the Activity; when the Activity is
+    // recreated the root component remounts here with a fresh initialState, which
+    // left the navigator without a valid state - a black screen on every reopen.
+    // Reuse the surviving store's state for the same route context instead.
+    if (react_native_1.Platform.OS === 'android' && storeRef.current.state && storeRef.current.context === context) {
+        initialState = storeRef.current.state;
+    }
     storeRef.current = {
         navigationRef,
         routeNode,
@@ -197,6 +206,7 @@ function useStore(context, linkingConfigOptions, serverUrl) {
         linking,
         redirects,
         state: initialState,
+        context,
     };
     if (initialState) {
         storeRef.current.routeInfo = getCachedRouteInfo(initialState);
