LANLINK_PORT=8787
LANLINK_DISCOVERY_PORT=8788

# Force the LAN address advertised to clients (QR / discovery beacon).
# Leave unset to auto-detect the real Wi-Fi/hotspot IP (ignores Docker/VPN).
# LANLINK_HOST=10.0.0.5

TRANSFER_CHUNK_SIZE=65536
TRANSFER_MAX_IN_FLIGHT_CHUNKS=32
